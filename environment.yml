name: flowguardgraph
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - streamlit
      - pyvis
      - networkx
      - pandas
      - pyyaml
      - pydantic
      - sqlglot
      - scikit-learn
      - matplotlib
      - pytest
      - ruff

